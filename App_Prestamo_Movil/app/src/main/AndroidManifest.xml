<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_prestamo_movil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_Prestamo_Movil">
        <activity android:name=".CobroActivity"></activity>
        <activity android:name=".PrestamoActivity" />
        <activity android:name=".NewClienteActivity" />
        <activity android:name=".NewPassActivity" />
        <activity android:name=".RestPassActivity" />
        <activity android:name=".NavegacionActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>